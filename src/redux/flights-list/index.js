import { combineReducers } from 'redux';
import flightPrice from './reducers';
// import { selectedFlightInfo, flightPrice } from './reducers';

export default combineReducers({
  // selectedFlightInfo,
  flightPrice,
});
